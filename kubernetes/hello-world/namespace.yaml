apiVersion: v1
kind: Pod
metadata:
  name: static-web
  labels:
    sagi: laufer
spec:
  containers:
    - name: web
      image: nginx
      ports:
        - name: web
          containerPort: 80
          protocol: TCP
