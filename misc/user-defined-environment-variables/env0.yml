version: 1

deploy:
  steps:
    terraformPlan:
      before:
        - echo USER_NAME=user >> $ENV0_ENV
        - echo USER_EMAIL=user@domain.com >> $ENV0_ENV
      after:
        - echo $USER_NAME
        - echo $USER_EMAIL
        - ./use-env-variables.sh
        - cd subdir ; ./use-env-variables.sh
