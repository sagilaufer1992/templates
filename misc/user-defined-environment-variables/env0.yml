version: 1

deploy:
  steps:
    terraformPlan:
      before:
        - echo $ENV
        - echo export USER_NAME=user >> $BASH_ENV
        - echo export USER_EMAIL=user@domain.com >> $BASH_ENV
      after:
        - cat $BASH_ENV
        - ./use-env-variable.sh
