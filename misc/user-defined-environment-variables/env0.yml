version: 1

deploy:
  steps:
    terraformPlan:
      before:
        - echo USER_NAME=user >> .env0-env-vars
        - echo USER_EMAIL=user@domain.com >> .env0-env-vars
      after:
        - cat .env0-env-vars
        - echo $USER_NAME
        - echo $USER_EMAIL
