version: 1

deploy:
  steps:
    terraformPlan:
      before:
        - npm upgrade -g npm
        - node --version
        - npm --version
        - yarn --version
        - git --version
        - python2 --version
        - python3 --version
        - pip2 --version
        - pip3 --version
        - aws --version
        - gcloud --version
        - az --version
        - kubectl version --client
        - tfenv --version
        - terraform --version
        - jq --version
        - pwsh -v
