version: 1

deploy:
  steps:
    terraformInit:
      after:
        - echo $HCL
        - echo YAML=`echo 'yamlencode($HCL)' | terraform console` >> $ENV0_ENV
        - echo $YAML
        - echo "-----"
        - cat $ENV0_ENV
