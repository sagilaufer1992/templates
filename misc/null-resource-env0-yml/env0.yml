version: 1

deploy:
  steps:
    terraformInit:
      after:
        -
        - echo HCL=`echo 'yamldecode(var.YAML)' | terraform console` >> $ENV0_ENV
        - echo $YAML
        - echo "-----"
        - cat $ENV0_ENV
