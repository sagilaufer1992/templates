version: 1

deploy:
  steps:
    setupVariables:
      after:
        - echo "{" >> a.txt
        - echo "\"a\":\"b\"" >> a.txt
        - echo "}" >> a.txt
        - echo x="${`cat a.txt`}" >> $ENV0_ENV
        - cat $ENV0_ENV
