version: 1

deploy:
  steps:
    terraformInit:
      after:
        - echo YAML=`echo 'yamlencode(var.HCL)' | terraform console` >> $ENV0_ENV
        - echo $YAML
        - echo "-----"
        - cat $ENV0_ENV
